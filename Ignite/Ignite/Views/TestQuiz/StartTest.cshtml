@model Ignite.ViewModels.Quiz

@using (Html.BeginForm("SubmitTest", "TestQuiz", FormMethod.Post, new { id = "test-form" }))
{
    @Html.HiddenFor(m => m.AssignmentId)

    @Html.EditorFor(m => m.Questions)
    <input type="submit" value="See Result" />
}

